{
    "name": "@interviewforge/shared",
    "version": "0.0.1",
    "private": true,
    "type": "module",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "build": "tsc",
        "type-check": "tsc --noEmit",
        "test": "jest --config ../../jest.config.cjs --selectProjects shared",
        "lint": "echo 'no linter configured yet'"
    },
    "dependencies": {
        "zod": "^3.23.0"
    },
    "devDependencies": {
        "typescript": "^5.7.0"
    }
}